eureka-credentials:
  username: admin
  password: admin
  
eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka-credentials.username}:${eureka-credentials.password}@0.0.0.0:8761/eureka/
      
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'                    